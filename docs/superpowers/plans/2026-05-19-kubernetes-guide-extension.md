# Kubernetes Guide Extension — Implementation Plan (Parts 10, 11, 12)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan phase-by-phase (subagent author → spec-compliance review → code-quality review → fix loop). Steps use checkbox (`- [ ]`) syntax.

**Goal:** Append 3 new Parts (~24 chapters) — 10 Cloud & Managed Kubernetes, 11 Advanced Production Patterns, 12 Kubernetes for ML — to the finished 50-chapter `full-guide/`, closing the genuine production/cloud/ML gaps, additively, without modifying the 50 existing chapters or the canonical app.

**Architecture:** Additive only. New dirs `full-guide/{10-cloud-and-managed-kubernetes,11-advanced-production-patterns,12-kubernetes-for-machine-learning}/` + new example trees (`examples/bookstore/{ml,operator,cloud,mesh}/`). Same Bookstore threaded (a "recommendations" model + a `BookstoreTenant` Kubebuilder operator). Only existing files touched: `README.md` TOC, `appendix/B-glossary.md`, `appendix/D-further-reading.md`. Every original hard invariant preserved & re-proven at the end.

**Tech Stack:** Markdown (9-section chapter anatomy); Mermaid; the existing Bookstore (Go/nginx + Postgres/Redis/RabbitMQ); new: NVIDIA GPU Operator, Kueue/Volcano/JobSet, Kubeflow Training Operator/Ray/KServe, Argo Workflows/Kubeflow Pipelines (Part 12); Kubebuilder/controller-runtime, Istio/Linkerd, Vault/External Secrets Operator, Karmada/ApplicationSet, Chaos Mesh/Litmus, Crossplane/Backstage (Part 11); eksctl/Terraform/Cluster API, IRSA/Workload Identity, Karpenter (Part 10). Validation: `kubectl apply --dry-run=client` (v1.35), `helm lint/template`, `kubectl kustomize`, `go vet`, `docker build`, python yaml, mermaid/anatomy/link/leak greps.

**Authoritative coverage:** delta-spec `docs/superpowers/specs/2026-05-19-kubernetes-guide-extension-design.md` §5 — every task below restates its coverage so tasks are self-contained.

---

## GLOBAL CONVENTIONS & HARD INVARIANTS (apply to EVERY task; verified each phase + final pass)

**Chapter anatomy (every new .md, in order):** 1 Title+1-line summary · 2 Why this exists · 3 Mental model · 4 Diagram(s) (Mermaid: `flowchart/graph/sequenceDiagram/stateDiagram-v2/classDiagram`, NO `timeline`; balanced ``` fences; `<br/>` not literal `\n`; NO emoji in node labels; + an ASCII diagram) · 5 Hands-on with the Bookstore (runnable, self-bootstrapping, repo-root-relative paths) · 6 How it works under the hood (real depth) · 7 Production notes (`> **In production:**`, incl. cloud EKS/GKE/AKS) · 8 Quick Reference (cmds + minimal manifest skeleton + checklist) · 9 Further reading (book from `docs/superpowers/specs/book-toc-reference.md` where applicable + an official docs URL). Cross-link by relative path; forward-refs OK.

**Additive-only:** NEVER edit the 50 existing chapters or `examples/bookstore/{raw-manifests,helm,kustomize,argocd,operators,cluster,app}`. New example artifacts go in NEW paths (`examples/bookstore/{ml,operator,cloud,mesh,multicluster,chaos}/` as needed). Only permitted existing-file edits: `README.md` (TOC), `appendix/B-glossary.md`, `appendix/D-further-reading.md` — extend, keep prior entries byte-stable.

**Preserve & re-prove (original invariants):** Helm default render **49**/0-CRD-miss; Kustomize **dev45/staging49/prod48**/0-CRD-miss; `helm lint` 0-failed; `DB_DSN` byte-identical (`raw-manifests/10`,`14`,catalog-rollout,inline) sort-u=1; no `cnpg`/`bookstore-db-rw` in raw|helm|kustomize|argocd; 4 app images `go vet`+`docker build` clean.

**PSA:** ns `bookstore` is `enforce: restricted`. Every NEW pod that can land there (ML jobs/serving, operator, mesh-injected, chaos, `kubectl run`/`kubectl debug`) is restricted-compliant (runAsNonRoot/non-root UID/allowPrivilegeEscalation:false/capabilities drop ALL/seccompProfile RuntimeDefault) OR runs in its own non-restricted namespace with a stated reason. System operators (GPU/Kueue/KServe/Istio/Vault/Crossplane/Karmada/Chaos-Mesh/Argo-Workflows) install into their OWN namespaces.

**Installs:** Helm with **pinned chart versions** (or stable non-version-pinned manifest) — NEVER `kubectl apply -f .../releases/latest/download/<PINNED-FILE>.yaml`. **CRD-backed manifests** carry the documented intrinsic note: "client dry-run shows `no matches for kind` until the operator/CRDs are installed; schema-correct" (same precedent as the guide's existing 18-/51-/70-/80-/83-/argocd/operators files).

**Honesty pattern:** where real GPUs / a cloud account / a real object-store / secret-manager is required, state it up-front as illustrative AND give a runnable CPU/local approximation where feasible (KServe CPU sklearn-class model; kind; local MinIO; Vault dev-mode). No machine-specific content (no usernames, `/Users/...`, `.colima`, "on this machine", real org/registry/email — generic labeled placeholders). Distroless app pods debugged via `kubectl debug --profile=restricted`, never `exec … sh`. Target k8s **v1.30+**; ignore false editor BrokenImport diagnostics.

**Per-phase validation (run every phase's validation step):** every new ```yaml full manifest → `kubectl apply --dry-run=client` (CRD-backed → documented `no matches` note present) ; new Go (operator/ML if Go) → `go vet`+`docker build` ; every ```mermaid fence valid header/balanced/`<br/>`/no-emoji/no-timeline ; all relative links resolve or match README-TOC ; 9 anatomy sections present ; leak scan `grep -rniE '/Users/|\.colima|on this machine|basit'` on the new dirs clean ; regression spot-check (helm 49 / kustomize 45-49-48 / DB_DSN sort-u=1 / no-cnpg-leak) UNCHANGED.

---

## File Structure

```
full-guide/
├── 10-cloud-and-managed-kubernetes/        01..06  (6 .md)
├── 11-advanced-production-patterns/        01..10  (10 .md)
├── 12-kubernetes-for-machine-learning/     01..08  (8 .md)
├── examples/bookstore/
│   ├── operator/   # Kubebuilder BookstoreTenant CRD+controller (Part 11 ch.02)
│   ├── mesh/        # service-mesh manifests (Part 11 ch.04)
│   ├── multicluster/# ApplicationSet/Karmada (Part 11 ch.06)
│   ├── chaos/       # Chaos Mesh/Litmus experiments (Part 11 ch.07)
│   ├── cloud/       # cloud IaC/identity/LB/storage samples (Part 10)
│   └── ml/          # recommendations: training, model, KServe IS, pipeline (Part 12)
├── README.md            (TOC extended — only)
└── appendix/{B-glossary.md, D-further-reading.md}   (extended — only)
```

---

## Phase X1 — Part 10: Cloud & Managed Kubernetes (6 chapters)

### Task X1: author `full-guide/10-cloud-and-managed-kubernetes/01..06`
- [ ] **X1.1 `01-managed-kubernetes-model.md`** — EKS/GKE/AKS architecture; shared-responsibility (provider owns control-plane/etcd; you own nodes/workloads/networking/IAM-wiring); control-plane SLA/versioning/auto-upgrade; managed vs self-managed (kubeadm/Cluster API) decision; regions/AZs, zonal vs regional clusters. Mermaid: shared-responsibility boundary (provider vs you) ; ASCII: managed-vs-self decision. Bookstore: where the Bookstore's pieces sit on a managed cluster (conceptual). Cite Rosso (deployment models) + the 3 providers' docs.
- [ ] **X1.2 `02-provisioning-and-iac.md`** — eksctl / `gcloud container clusters` / `az aks`; Terraform (cluster+nodepool modules); Cluster API (mgmt vs workload clusters); managed node groups/pools & upgrades & surge; reproducible cluster IaC; honest "needs a cloud account" + accurate commands. Mermaid: IaC→cluster→nodepools flow ; ASCII: provisioning-tool matrix. Bookstore: provision a cluster + deploy the Bookstore (kustomize prod overlay with the kind-image caveat reused). Cite Rosso + eksctl/Terraform/Cluster-API docs.
- [ ] **X1.3 `03-cloud-identity.md`** — the OIDC trust model; **IRSA** (EKS), **GKE Workload Identity**, **AKS Workload Identity**, **EKS Pod Identity**; map K8s ServiceAccount → cloud IAM role; least-privilege scoping; no static keys; token projection. Mermaid: SA→OIDC→cloud-IAM sequence ; ASCII: the 4 mechanisms compared. Bookstore: give `catalog`/`orders` SAs cloud creds (e.g. read an S3/GCS bucket) with zero static keys. Cite provider IAM docs + Rosso security.
- [ ] **X1.4 `04-cloud-networking-and-load-balancing.md`** — VPC CNI & alternatives, pod-CIDR/IP-exhaustion, private clusters/egress; AWS Load Balancer Controller (ALB/NLB), GKE/AKS LB & Ingress/Gateway; ExternalDNS; cloud NetworkPolicy enforcement; **eBPF/Cilium on cloud** (the eBPF-depth gap). Mermaid: internet→cloud-LB→Ingress/Gateway→Service→Pod on a managed cluster ; ASCII: LB-type/annotation table. Bookstore: expose storefront via a cloud LB + ExternalDNS (described, honest). Cite provider networking docs + Cilium docs.
- [ ] **X1.5 `05-cloud-storage-and-data.md`** — cloud CSI (EBS/PD/Azure Disk; EFS/Filestore/Azure Files for RWX), StorageClass params & `volumeBindingMode`, snapshots, cross-AZ/regional disk constraints, expansion; the Bookstore Postgres on a cloud disk + cloud snapshot/backup (ties to Part 08 ch.02, no canonical edit). Mermaid: PVC→cloud-CSI→cloud-disk + snapshot ; ASCII: block-vs-file/RWX matrix. Cite provider CSI docs + Rosso storage.
- [ ] **X1.6 `06-node-autoscaling-cost-multicloud.md`** — Cluster Autoscaler vs **Karpenter** (provisioners/NodePools, consolidation, spot, bin-packing) / GKE Autopilot / AKS node autoprovision; spot/preemptible strategy & interruption handling; cloud cost & FinOps (cost dashboards, Reserved/Savings, rightsizing ties to Part 06 ch.06); multi-cloud/hybrid & portability (what's portable, what isn't); cloud production-readiness checklist. Mermaid: Karpenter unschedulable-pod→provision→consolidate loop ; ASCII: CA-vs-Karpenter / cost-lever table. Cite Karpenter docs + provider cost docs.
- [ ] **X1.7 Phase X1 validation:** per-phase validation block (above) over `10-*`+any `examples/bookstore/cloud/` sample manifests; report.

---

## Phase X2 — Part 11: Advanced Production Patterns (10 chapters, 3 sub-phases)

### Task X2a: `11-advanced-production-patterns/01..03`
- [ ] **X2a.1 `01-admission-webhooks.md`** — admission pipeline recap (mutating→validating, vs Kyverno from Part 05 ch.03); build a real **ValidatingWebhookConfiguration + MutatingWebhookConfiguration** server for the Bookstore (e.g. enforce required labels / inject a default) — Go webhook in `examples/bookstore/operator/` or a dedicated dir; TLS/cert via cert-manager or self-signed (documented), `failurePolicy`, `sideEffects`, `matchConditions`, `reinvocationPolicy`, ordering, timeouts, testing & fail-safe; in-tree **ValidatingAdmissionPolicy + CEL** as the webhook-less modern alternative. Mermaid: request→mutate→validate(webhook+VAP)→persist ; ASCII: webhook-vs-VAP tradeoffs. CRD/webhook manifests carry the CRD-intrinsic-style note (needs the webhook deployed). Cite k8s admission docs + Rosso.
- [ ] **X2a.2 `02-operator-development.md`** — **Kubebuilder** hands-on building `examples/bookstore/operator/`: a `BookstoreTenant` CRD `bookstore.example.com/v1alpha1` whose controller reconciles a minimal restricted-compliant slice (Deployment+Service+ConfigMap) into a tenant namespace; project layout, `make manifests/generate`, the reconcile loop (level-triggered/idempotent), owner refs, finalizers, status conditions+events, `v1alpha1→v1beta1` conversion webhook, **envtest** unit tests, packaging (Helm/OLM); contrast Part 08 ch.05 "using an operator". The operator is real Go: `go vet` + `docker build` clean; CRD/sample carry the CRD-intrinsic note. Mermaid: reconcile loop (watch→diff→act→status) ; ASCII: Kubebuilder project tree. Cite Ibryam (Operator/Controller) + Kubebuilder book/docs.
- [ ] **X2a.3 `03-api-priority-and-fairness.md`** — APF: FlowSchema + PriorityLevelConfiguration, request classification (by user/SA/verb/resource), queuing/fair-sharing/concurrency shares, `exempt`/`catch-all`, protecting the control plane from a noisy tenant/controller; APF metrics (`apiserver_flowcontrol_*`) & tuning; interaction with client-side rate limiting & priority. Mermaid: request→FlowSchema match→PriorityLevel queue→handler ; ASCII: default flow-schemas & where a tenant's bulk lister lands. Bookstore: classify the BookstoreTenant operator's traffic into a bounded priority level. Cite k8s APF docs.
- [ ] **X2a.4 validation:** per-phase block over `11-01..03` + `examples/bookstore/operator/` (go vet + docker build the operator/webhook; CRD/webhook dry-run shows documented note); report.

### Task X2b: `11-advanced-production-patterns/04..07`
- [ ] **X2b.1 `04-service-mesh.md`** — why a mesh; data plane (sidecar vs **Istio ambient**/ztunnel; Linkerd micro-proxy); mTLS/SPIFFE identity (the SPIFFE/SPIRE gap lands here), traffic management (mesh-layer canary/mirroring/retries/outlier-detection) vs Gateway API (Part 02 ch.05) — when which; mesh observability (golden signals/distributed tracing tie to Part 06); cost/complexity/when-NOT. Install a mesh on a kind cluster (pinned Helm), enable mTLS between Bookstore services, a mesh canary on `catalog`. New manifests in `examples/bookstore/mesh/` (PeerAuthentication/VirtualService-or-ambient equivalents) — CRD-intrinsic note. Mermaid: mesh data/control plane + mTLS handshake ; ASCII: sidecar-vs-ambient / mesh-vs-GatewayAPI. Cite Istio/Linkerd docs.
- [ ] **X2b.2 `05-secrets-at-scale.md`** — the demo-Secret problem at scale; **External Secrets Operator** + a backend (HashiCorp **Vault** dev-mode runnable locally, and cloud SM conceptually); Vault k8s auth, dynamic secrets, agent/sidecar injector, rotation; SOPS/Sealed-Secrets recap (Part 07 referenced); the Bookstore DB creds sourced from Vault/ESO as the **production replacement** for `16-db-credentials.yaml` — ADDITIVE (`examples/bookstore/...` new ExternalSecret/SecretStore; canonical Secret untouched; show the swap + revert). CRD-intrinsic note for ESO/Vault CRs. Mermaid: ESO→SecretStore→Vault→synced Secret→Pod ; ASCII: rotation/lease lifecycle. Cite ESO + Vault docs + Rosso security.
- [ ] **X2b.3 `06-multi-cluster-and-fleet.md`** — why/when multi-cluster; topologies (per-env/region/tenant; hub-spoke); **Argo CD ApplicationSet** (cluster generators) delivering the Bookstore to N clusters; **Karmada** / Cluster API fleet; cross-cluster service discovery & networking (mesh multi-cluster, Submariner) ; failover/DR & data gravity. New `examples/bookstore/multicluster/` (ApplicationSet + a 2-cluster kind topology, honest). Mermaid: hub Argo→fleet of clusters reconcile ; ASCII: multi-cluster topology options. CRD-intrinsic note (ApplicationSet/Karmada CRDs). Cite Argo CD + Karmada docs.
- [ ] **X2b.4 `07-chaos-engineering.md`** — resilience-engineering principles (steady-state hypothesis, blast radius, abort, game days); **Chaos Mesh** (or Litmus) pinned-Helm install; experiments against the running Bookstore — pod-kill, network latency/loss, IO fault, CPU/mem stress, time skew — observed against the Part-06 SLOs/PDBs; making chaos a CI gate. New `examples/bookstore/chaos/` experiment CRs (restricted-safe; CRD-intrinsic note). Mermaid: hypothesis→inject→observe→learn loop ; ASCII: experiment catalog. Cite Chaos Mesh/Litmus + SRE workbook.
- [ ] **X2b.5 validation:** per-phase block over `11-04..07` + `examples/bookstore/{mesh,multicluster,chaos}/`; report.

### Task X2c: `11-advanced-production-patterns/08..10`
- [ ] **X2c.1 `08-ha-control-plane-and-etcd.md`** — HA topology (stacked vs external etcd, ≥3 control-plane nodes, API LB), etcd raft/quorum sizing (why odd, failure tolerance), etcd maintenance (defrag, auto-compaction, space quota, `NOSPACE` alarm recovery), etcd DR beyond snapshot (member replace, full restore drill — extends Part 08 ch.02 conceptually, no canonical edit), control-plane node-loss recovery, component leader election (`Lease`). Mermaid: HA CP + etcd quorum ; ASCII: quorum/failure-tolerance table. Cite k8s HA + etcd ops docs.
- [ ] **X2c.2 `09-performance-and-scalability.md`** — apiserver (watch cache, `--max-requests-inflight`, APF tie), etcd (disk/latency), kubelet/scheduler tuning; Kubernetes scalability thresholds & the official SLOs; pprof profiling; load/scale testing with **kube-burner / clusterloader2**; large-cluster pitfalls (endpoints, large lists, secrets-per-node); the Bookstore under synthetic load. Mermaid: where latency comes from (client→APF→apiserver→etcd) ; ASCII: scalability-limit cheat-table. Cite k8s scalability SIG docs.
- [ ] **X2c.3 `10-platform-engineering.md`** — internal developer platforms; **Crossplane** (cloud infra as k8s APIs/Compositions/Claims), **Backstage** (catalog/golden-paths/scaffolder), Score/portable workload spec; self-service + the guardrail stack (PSA/RBAC/quota/policy/GitOps from Parts 04–08) as a paved road; building a Bookstore "golden path" (a Backstage template / Crossplane Composition that yields a compliant Bookstore env — conceptual + a runnable Crossplane example where feasible, honest). Mermaid: developer→portal→template→GitOps→guarded cluster ; ASCII: platform capability stack. CRD-intrinsic note (Crossplane/Backstage). Cite Crossplane/Backstage + platform-engineering refs.
- [ ] **X2c.4 validation:** per-phase block over `11-08..10`; report.

---

## Phase X3 — Part 12: Kubernetes for Machine Learning (8 chapters, 3 sub-phases) + `examples/bookstore/ml/`

### Task X3a: `12-kubernetes-for-machine-learning/01..03` + the ml/ skeleton
- [ ] **X3a.1 `01-why-ml-on-kubernetes.md`** — ML workload taxonomy (notebooks/interactive, batch/training, HPO, batch inference, online serving, pipelines); why k8s for ML (scheduling/packaging/scaling/reproducibility/multi-tenancy); the MLOps loop; what's different vs stateless web (GPUs, gang scheduling, data gravity, long jobs, cost, determinism); introduce the Bookstore **recommendations** model (item-to-item "bought X → Y" from catalog/orders). Mermaid: the MLOps loop (data→train→register→serve→monitor→retrain) ; ASCII: ML-workload taxonomy. Create `examples/bookstore/ml/README.md` + the recommendations dataset/feature definition stub. Cite Kubeflow/MLOps refs.
- [ ] **X3a.2 `02-gpus-and-accelerators.md`** — device-plugin model, **NVIDIA GPU Operator** (driver/toolkit/device-plugin/DCGM), requesting `nvidia.com/gpu`, **MIG**, **time-slicing & MPS** for sharing, GPU node pools + taints/labels + GPU-aware scheduling, GPU monitoring (DCGM→Prometheus, ties Part 06), cost implications. Honest: needs real GPU nodes — give the reasoning + a CPU-only fallback path for later chapters. Mermaid: kubelet→device-plugin→GPU allocation ; ASCII: full-GPU vs MIG vs time-slice vs MPS. Cite NVIDIA GPU Operator docs.
- [ ] **X3a.3 `03-batch-and-gang-scheduling.md`** — Indexed Job → **JobSet** (the JobSet gap), the gang/all-or-nothing problem; **Kueue** (ClusterQueue/LocalQueue/ResourceFlavor, quotas, cohorts, fair-sharing, preemption, `suspend`); **Volcano** (PodGroup/gang, queues); choosing Kueue vs Volcano; multi-tenant GPU quota for the Bookstore ML namespace. Mermaid: Kueue admission/quota/preemption flow ; ASCII: JobSet/Kueue/Volcano roles. New `examples/bookstore/ml/` Kueue queue + a JobSet sample (CRD-intrinsic note; restricted-safe). Cite Kueue/JobSet/Volcano docs.
- [ ] **X3a.4 validation:** per-phase block over `12-01..03` + `examples/bookstore/ml/` so far; report.

### Task X3b: `12-kubernetes-for-machine-learning/04..06`
- [ ] **X3b.1 `04-distributed-training.md`** — **Kubeflow Training Operator** (PyTorchJob/TFJob, master/worker, gang via Kueue/Volcano), **Ray/KubeRay** (RayCluster/RayJob), data loading & checkpointing to a PVC (snapshots for reproducibility, ties Part 03 ch.05), elastic/fault-tolerant training; **actually train the tiny recommendations model** in `examples/bookstore/ml/train/` (small, CPU-runnable real code: e.g. implicit-feedback item-kNN/ALS in scikit-learn-class libs; a `PyTorchJob`/`RayJob` manifest shown, plus a plain Job CPU path that runs without GPUs). Mermaid: distributed-training topology (chief/workers/PS or Ray head/workers) ; ASCII: framework-operator matrix. CRD-intrinsic note. Cite Kubeflow Training/Ray docs.
- [ ] **X3b.2 `05-notebooks-and-interactive.md`** — JupyterHub on k8s / Kubeflow Notebooks; per-user isolation/quota/PVC/GPU; the **restricted-PSA reality** for notebooks (must run non-root/hardened — show how), culling/idle, data access & secrets, the dev→train→serve workflow from a notebook to the Bookstore recommender. Mermaid: JupyterHub spawner→per-user pod ; ASCII: notebook isolation/quota knobs. Pinned-Helm install; restricted-safe. Cite JupyterHub/Kubeflow Notebooks docs.
- [ ] **X3b.3 `06-model-serving-and-inference.md`** — **KServe** `InferenceService` (predictor/transformer/explainer, serverless Knative vs raw deploy), Seldon & **NVIDIA Triton** overview; autoscaling inference (Knative/HPA/KEDA on RPS/latency, **scale-to-zero**), GPU sharing for serving, model **canary/A-B** (ties Part 07 ch.05); serve the trained recommendations model and have `catalog`/`storefront` call it — a real **CPU sklearn-class `InferenceService`** in `examples/bookstore/ml/serve/` that runs on kind (no GPU); restricted-safe; CRD-intrinsic note. Mermaid: request→KServe(transformer→predictor)→response + autoscale ; ASCII: KServe-vs-Seldon-vs-Triton. Cite KServe docs.
- [ ] **X3b.4 validation:** per-phase block over `12-04..06` + `examples/bookstore/ml/{train,serve}/` (the CPU InferenceService + training Job dry-run/parse; any Python `python -m py_compile`); report.

### Task X3c: `12-kubernetes-for-machine-learning/07..08`
- [ ] **X3c.1 `07-ml-pipelines-and-workflows.md`** — **Argo Workflows** / **Kubeflow Pipelines** (DAG, artifacts, caching); the recommendations **train→evaluate→register→deploy** pipeline; data/feature reproducibility (volume snapshots, dataset/version lineage), a model registry (MLflow/KServe ModelMesh overview); GitOps-for-models (the pipeline emits a manifest Argo CD syncs — ties Part 07 ch.04). New `examples/bookstore/ml/pipeline/` (Workflow/Pipeline manifest; CRD-intrinsic note; restricted-safe). Mermaid: the ML DAG ; ASCII: pipeline-tool comparison. Cite Argo Workflows/Kubeflow Pipelines docs.
- [ ] **X3c.2 `08-ml-platform-cost-and-mlops.md`** — GPU cost/FinOps (utilization, MIG/time-slice sharing, spot for training, scale-to-zero for serving, ties Part 06 ch.06 & Part 10 ch.06), ML multi-tenancy & fairness (Kueue + ResourceQuota + RBAC + PSA), ML observability (training metrics, **data/model drift**, serving SLOs/latency, GPU saturation), and the **end-to-end MLOps capstone**: stand up the Bookstore recommender from data→Kueue-scheduled training→registered model→KServe scale-to-zero serving→consumed by storefront→monitored→retrain trigger — pure orchestration of Part-12 artifacts (no new app manifests), honest GPU/cloud notes, a Part-12 production-readiness checklist mapping each ML concern → its chapter. Mermaid: the full MLOps platform architecture ; ASCII: ML cost levers / readiness checklist. Cite Kubeflow/MLOps + SRE-for-ML refs.
- [ ] **X3c.3 validation:** per-phase block over `12-07..08` + `examples/bookstore/ml/pipeline/`; report.

---

## Phase X4 — Finalize: README/appendix + guide-wide consistency pass

### Task X4: integrate & re-prove the whole extended guide
- [ ] **X4.1 README TOC** — extend `full-guide/README.md` TOC with Parts 10/11/12 (exact new chapter paths) + a one-line note the appendix covers the new domains. Keep ALL existing TOC entries byte-stable. Validate every new TOC link resolves.
- [ ] **X4.2 appendix B-glossary** — extend `full-guide/appendix/B-glossary.md` with the new terms (GPU/MIG/MPS/time-slicing/device-plugin, Kueue/Volcano/JobSet/gang-scheduling, KServe/Seldon/Triton/InferenceService, Ray/KubeRay, Kubeflow, IRSA/Workload-Identity/Pod-Identity, Karpenter, Crossplane, Backstage, Vault/External-Secrets-Operator, admission-webhook/ValidatingAdmissionPolicy/CEL, API-Priority-and-Fairness/FlowSchema/PriorityLevelConfiguration, Kubebuilder/controller-runtime/envtest, Cluster-API/Karmada, Chaos-Mesh/Litmus, eBPF/Cilium, SPIFFE/SPIRE, MLOps/model-drift/model-registry), each linked to the new chapter that defines it; existing entries byte-stable; update the seed/expansion note.
- [ ] **X4.3 appendix D-further-reading** — add Parts 10/11/12 rows: books from `docs/superpowers/specs/book-toc-reference.md` where applicable + official docs (cloud-provider, Istio/Linkerd, Vault/ESO, Crossplane/Backstage, Kubebuilder, KServe/Kubeflow/Ray/Kueue/Volcano, Argo Workflows, Chaos Mesh, Karpenter, Cluster API/Karmada). No invented sources. Existing rows byte-stable.
- [ ] **X4.4 Guide-wide consistency pass over the EXTENDED guide** (same 14-check rigor as the original 8c): (1) link graph all `full-guide/**/*.md` 0-dangling (incl. new↔old cross-refs & README/appendix); (2) README TOC lists all 74 chapters + appendix, all resolve; (3) anatomy: all 24 NEW chapters have the 9 sections (existing 50 untouched — spot-confirm unchanged); (4) every ```mermaid valid/balanced/`<br/>`/no-emoji/no-timeline guide-wide; (5) every new ```yaml/manifest dry-runs (CRD-backed → documented note present) + all new example trees parse; (6) bookstore coherence + **regression re-proof**: `helm lint` 0-failed, helm **49**/0-miss, kustomize **dev45/staging49/prod48**/0-miss, `DB_DSN` sort-u=**1**, no `cnpg`/`bookstore-db-rw` in raw|helm|kustomize|argocd, only README+appendix-B+appendix-D modified among pre-existing files (mtime/diff check); (7) new pods in ns `bookstore` restricted-safe (server-dry-run if a kind cluster exists; clean up); (8) distroless-debug invariant; (9) installs Helm/pinned, no fragile `latest/download`; (10) CRD-intrinsic notes present on every new CRD-backed manifest; (11) `examples/bookstore/{operator,ml}` Go (if any) `go vet`+`docker build`; the 4 original app images still `go vet`+`docker build` clean; (12) leak scan whole guide empty; (13) no `timeline` mermaid; (14) residual-findings list. Fix defects surgically (new files only; never the 50 chapters/canonical app). Report DONE/DONE_WITH_FINDINGS + the invariant re-proof numbers.

---

## Self-Review (plan author, against the delta-spec)

**1. Spec coverage:** delta-spec §5 Part 10 (6 ch) → X1.1–X1.6 ✓; Part 11 (10 ch) → X2a.1-3 + X2b.1-4 + X2c.1-3 ✓ (admission-webhooks, operator-dev/Kubebuilder, APF, service-mesh, secrets-at-scale, multi-cluster, chaos, HA-etcd, perf/scale, platform-eng — all 10 mapped); Part 12 (8 ch) → X3a.1-3 + X3b.1-3 + X3c.1-2 ✓ (why, GPUs, batch/gang, distributed-training, notebooks, serving, pipelines, cost/MLOps-capstone). §4 Bookstore threading → recommendations model (X3a.1/X3b.1/X3b.3/X3c) + BookstoreTenant operator (X2a.2) + mesh/secrets/chaos/multicluster threading (X2b) + cloud Bookstore (X1) ✓. §6 existing-file updates → X4.1/X4.2/X4.3 ✓. §3 invariants → GLOBAL block + every phase validation + X4.4 re-proof ✓. §7 phasing → X1/X2(a,b,c)/X3(a,b,c)/X4 ✓. No gaps.
**2. Placeholder scan:** No TBD/TODO/"handle edge cases". Each chapter task = concrete coverage bullets + named Mermaid+ASCII + explicit Bookstore increment + citation target + validation; prose generated at execution against the fixed anatomy + bullets (same model as the original plan, not deferred).
**3. Name/path consistency:** Part dirs `10-cloud-and-managed-kubernetes`/`11-advanced-production-patterns`/`12-kubernetes-for-machine-learning`; new example dirs `examples/bookstore/{cloud,operator,mesh,multicluster,chaos,ml}` used identically across tasks & X4; the operator is consistently `BookstoreTenant` `bookstore.example.com/v1alpha1`; the ML thread is consistently the "recommendations" model with `ml/{train,serve,pipeline}`; invariant numbers (49/45/49/48, DB_DSN sort-u=1) identical in GLOBAL, every phase, and X4.4. No drift.

No issues requiring change. Plan ready; execute via subagent-driven-development.
