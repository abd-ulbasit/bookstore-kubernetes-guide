# config/crd — the CRD only. `make install` does:
#   kustomize build config/crd | kubectl apply -f -
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - bookstore.example.com_bookstoretenants.yaml
